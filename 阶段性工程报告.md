# 阶段性工程报告（2026-02-21，已按最新决议更新）

## 1. 项目定位
- 研究目标：提升 LLM 在给定知识约束下的问答可靠性（grounded factuality）。
- 当前主路线：`ssqpg`（自采样回答 + NLI 判别 + 过滤配对 + 难题增广）。
- 搁置路线：`meqng`（保留代码与入口，仅兼容，不作为主实验路径）。

## 2. 当前主流程（有效）
1. `ssqpg source sample`：源数据抽样与长度约束。
2. `ssqpg generate answers`：同题多次采样回答（local/api）。
3. `ssqpg judge answers`：NLI + QA 语义一致性判别。
4. `ssqpg pair build`：答案级过滤 + hard 问题增广/修复（已在此阶段执行）。
5. `ssqpg filter apply`：导出最终 DPO pair。
6. `ssqpg audit surface`：表面特征可分性审计。
7. `ssqpg bucket difficulty`：按 log-prob gap 分桶。

## 3. 本次已执行修改

### 3.1 已修复：`model_eval` 写文件硬错误
- 问题：5 个脚本中 `write_jsonl` 参数顺序写反，运行时会报错。
- 已修：统一改为 `write_jsonl(path, records)`。
- 涉及文件：
  - `src/model_eval/generate_answers.py`
  - `src/model_eval/eval_nli_faithfulness.py`
  - `src/model_eval/eval_deepeval_hallucination.py`
  - `src/model_eval/eval_nli_deepeval_consistency.py`
  - `src/model_eval/search_nli_deepeval_thresholds.py`

### 3.2 已删除：`ssqpg/repair` 遗存主逻辑
- 决策：hard 增广/修复统一在 `pair build`（`hard_augment`）执行，`repair` 子流程退场。
- 已执行：
  - 删除 `src/ssqpg/repair.py`
  - 删除 `ssqpg` CLI 的 `repair` 子命令入口与相关代码
  - 删除 `RepairConfig`（`src/ssqpg/config.py`）
  - 同步调整 CLI stage 文案编号（过滤/审计/分桶顺延）

### 3.3 已补充：依赖与执行前构建说明
- `README.md` 已改为 `ssqpg` 主流程说明，并加入 `uv` 构建与运行前准备步骤。
- `pyproject.toml` 已补充可选依赖：
  - `lora`：`peft`
  - `eval`：`deepeval`
- 同时保留并说明 `meqng` 为兼容入口，避免影响现有 uv/venv 识别。

### 3.4 新增：`cablibrate` 校准子包（与 `model_eval` 解耦）
- 目标：将 NLI 校准/策略搜索能力从 `model_eval` 拆出，形成独立实验工具链，默认面向“外部可靠标注数据集”。
- 新增包与脚本（`src/cablibrate/`）：
  - `score_nli_for_calibration.py`
  - `search_calibration_strategies.py`
  - `temperature_scale_and_search.py`
  - `common.py`
- 兼容保留：原 `src/model_eval/score_nli_from_deepeval_details.py`、`src/model_eval/search_nli_deepeval_split_strategies.py`、`src/model_eval/temperature_scale_nli_and_search.py` 仅作为轻量转发入口。

#### 3.4.1 数据接口与解耦说明
- 新接口不再依赖 DeepEval 产物格式，可通过字段参数接入外部标注数据：
  - `knowledge_field/question_field/answer_field/label_field`
  - `positive_label_values/negative_label_values`
  - 允许可选模型元字段（`model_tag/model_step/model_path`）用于分组统计。
- 核心假设：外部标签是可靠监督信号（`gold_supported ∈ {True, False}`），用于校准与阈值选择。

#### 3.4.2 策略搜索核心算法（本次重点）
- 记 `p_e, p_n, p_c` 为 NLI 输出的 entail/neutral/contradict 概率。
- 方法 A（2.1）：`argmax(p_e, p_n, p_c)`，且仅 `argmax==entail` 判为正类。
- 方法 B（2.2）：先做 logit（实现中为 `log(p)` 等价单调变换），定义
  - `margin = logit_e - max(logit_n, logit_c)`，
  - 当 `margin >= τ` 判为正类，搜索阈值 `τ`。
- 方法 C（3.1，带拒答）：在方法 A 基础上加置信度门控：
  - 若 `max(p_e, p_n, p_c) < γ` 则拒答，否则按 A 判类。
- 方法 D（3.2，带拒答）：在方法 B 基础上加“边界带”拒答：
  - 若 `|margin - τ| <= δ` 则拒答，否则按 B 判类。

#### 3.4.3 拒答策略优化目标（拉格朗日形式）
- 对 C/D 使用：
  - `L = Error(kept) + α * RejectRate`
- 其中：
  - `Error(kept) = 1 - Accuracy(kept)`
  - `RejectRate = rejected / labeled`
- 通过网格搜索阈值组合（`γ` 或 `τ, δ`）最小化 `L`，在准确率与覆盖率间求平衡。

#### 3.4.4 Temperature Scaling 逻辑
- 对每个模型（或全局）拟合温度 `T`：
  - 将三分类概率转为 log 空间分数 `log(p)`，再做 `softmax(log(p)/T)` 得到校准概率。
  - 使用外部二值标签（支持/不支持）在 `p_entail(T)` 上最小化 binary NLL。
- 拟合后基于校准分数再次运行 A/B/C/D 搜索，输出“校准前后可比较”的最优策略结果。

## 4. 配置集中化（暂缓，已记录）
- 现状：CLI 参数与阈值配置仍分散，适合阶段性实验与逐段验证。
- 决议：暂不收敛为全流程一键执行与统一配置中心；待工程功能稳定后再做。

## 5. `meqng` 遗留问题盘点（按“易改/暂缓”）

### 5.1 已做的低风险修改
- README 从旧 `meqng` 运行示例切换为 `ssqpg` 主流程。
- `pyproject` 描述明确 `ssqpg` 为主 CLI、`meqng` 为 legacy。

### 5.2 暂缓的高影响项（重建工程时统一处理）
- 包名 `meqng`（`pyproject.toml` 的 `project.name`）；
- legacy 脚本入口 `meqng = meqng.cli:app`；
- `src/meqng` 与 `src/meqng.egg-info` 的整体清理/重命名。

上述项会影响环境识别、入口兼容与安装缓存，当前阶段不建议单点修改。

## 6. 仍未完成的主缺口
- DPO 训练重写尚未接回（`src/dpo_trainer/` 为空），当前仍停留在“数据构造 + 评估工具”阶段。
- 完整闭环（构造 -> 训练 -> 前后测）需在训练模块重建后再打通。
