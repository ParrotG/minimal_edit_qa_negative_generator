# 阶段性工程报告

## 1. 当前状态

仓库已完成一次方向性收敛：**彻底转为 grounded-QA 的 SFT-only 工程**。

当前保留的主线只有：

- source 构造
- teacher structured completion 生成与验证
- SFT 数据集准备与训练
- validation / test 评测与报告

以下旧路线已删除：

- DPO 训练与配套数据构造
- `meqng`
- `ssqpg`
- DPO-only 的 preference / faithfulness / threshold-search 评测脚本

## 2. 本轮重构内容

### 2.1 双源 Hotpot tagging

source 入口已改为同时读取 HotpotQA distractor 的：

- `train`
- `validation`

然后分别打标签：

- Hotpot `train` -> `train_sft_raw`
- Hotpot `validation` -> 再切成 `validation` / `test`

同时保留独立的 `answerability_split`：

- `answerable`
- `unanswerable`
- `both`

### 2.2 source / teacher 工作流固定

当前固定流程为：

`source tag -> source build -> source prefilter -> source partition -> teacher generate -> teacher validate -> build sft-records`

特点：

- source prefilter 统一承担 infer prompt token 预算过滤
- unanswerable 的 NLI prefilter 也放在 source 阶段
- teacher validate 接受 mixed candidates，并在内部按 `answerability_label` 分流

### 2.3 model_eval 对齐当前协议

当前 `model_eval` 已与 Grounded-QA 协议对齐，包含：

- `eval_sft_loss_curve`
- `generate_structured_answers`
- `eval_grounded_qa`
- `generate_answers`
- `eval_task_content_baseline`
- `eval_deepeval_hallucination`
- `merge_eval_curves`
- `run_sft_eval_report`

其中新增的关键能力包括：

- `eval_track / eval_variant` 元数据统一
- DeepEval 的 structured 模式
- confidence reliability AUROC
- validation 自动选最佳 ckpt，并在 test 上打包四条对比轨道

### 2.4 仓库裁剪为 SFT-only

已清理：

- `src/dpo_trainer`
- `src/meqng`
- `src/ssqpg`
- `src/meqng.egg-info`
- DPO-only `model_eval` 文件
- 旧 README 与旧路线文档

`pyproject.toml` 也已同步改为 grounded-QA SFT 元数据。

## 3. 当前验证口径

### 3.1 structured grounded eval

`eval_grounded_qa` 当前关注：

- protocol compliance
- answerability accuracy
- evidence substring legality
- correctness strict / reviewed
- semantic yes rate / margin
- confidence reliability AUROC

### 3.2 base baseline

当前保留三类 base baseline：

- `base_protocol`
- `base_task_no_think`
- `base_task_think`

并与 `best_ckpt_structured` 一起进入最终 test 报告。

## 4. 当前主命令集

```bash
python -m grounded_qa.cli source tag ...
python -m grounded_qa.cli source build ...
python -m grounded_qa.cli source prefilter ...
python -m grounded_qa.cli source partition ...

python -m grounded_qa.cli teacher generate ...
python -m grounded_qa.cli teacher validate ...
python -m grounded_qa.cli build sft-records ...

python -m sft_trainer.prepare_sft_dataset ...
python -m sft_trainer.train_sft ...

python -m model_eval.run_sft_eval_report ...
```

## 5. 剩余关注点

本轮重构后，剩余主要工作重点不再是工程拆分，而是实验口径细化：

- matcher / answer equivalence 的进一步校准
- DeepEval 指标与人工校验的一致性分析
- 不同 ckpt 在 answerability / correctness / semantic 上的误差分析
