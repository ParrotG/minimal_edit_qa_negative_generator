# 阶段性工程报告（2026-02-22）

## 1. 工程现状简述

- 主流程：`ssqpg`（source -> generate -> judge -> pair stages -> filter -> audit -> bucket）。
- 评估与校准工具：
  - `model_eval` 用于评估与分析；
  - `cablibrate` 用于 NLI 校准与策略搜索（与 `model_eval` 解耦）。
- 当前判定核心包：`qa_judge`（原 `nli_judge` 已重命名）。
- 训练侧状态：DPO 训练脚本仍为独立阶段，尚未与主流程一键闭环联动。
- legacy 路线：`meqng` 仍保留，仅用于兼容，不是主实验路径。

## 2. 近期更新（滚动）

### 2.1 判定系统重构（本轮重点）

- `nli_judge` 全面重命名为 `qa_judge`，并同步替换下游导入。  
- judge 内核改为“单 NLI 验证器 + 温度缩放 + 双策略并行输出”：
  - 全量二分类：方法 B（`logit_margin_threshold`）；
  - 可拒答三分类：方法 D（`margin_with_band_reject`）。
- 默认参数来自 `outputs/test_run/nli_deepeval_split_search_calibrated_best.jsonl`：
  - `temperature=3.0`
  - B: `margin_threshold=0.6`
  - D: `margin_threshold=0.9`, `band_half_width=0.95`
- judge 输出新增并统一记录：
  - `margin`（校准后）
  - `full_binary` 与 `reject_aware` 两套决策
  - `qa_consistency` 检查结果
- 已移除 judge 内 secondary/reference 相关逻辑与调用链。

### 2.2 QA 一致性逻辑集中化

- 原散落于 `ssqpg/filtering.py`、`ssqpg/answer_filter.py`、旧 judge 的 QA 判定逻辑已收敛至 `qa_judge/qa_consistency.py`。  
- `NERTagger` 与 answer-type 检查一起迁移到 `qa_judge`，避免跨包重复实现。  
- QA 检查结果目前保留在 judge 信息中；可作为后续策略试验的附加信号。

### 2.3 ssqpg 流程拆分与配对逻辑调整

- `pair build` 已拆分为四段子流程：
  - `pair answer-filter`
  - `pair group`
  - `pair hard-augment`
  - `pair pairing`
- Stage 3 采用可拒答判定；后续 `answer-filter` 默认丢弃 `abstain`。  
- `group` 逻辑从 `pairing` 抽离到 `ssqpg/grouping.py`；`pairing` 仅负责组内匹配。  
- 支持度依据由 `candidate_entail_primary` 改为 judge `margin`；负样本选择改为偏好更低支持度（更低 margin）。

### 2.4 最终过滤器重构

- `ssqpg/filtering.py` 已改名为 `ssqpg/pair_filter.py`。  
- 禁用最终阶段 NLI 重算，改为消费已有记录分数（`pair_meta` 中的 margin）。  
- NLI 翻转条件改为 margin gap 过滤：
  - `chosen_margin - rejected_margin >= PairFilterConfig.min_margin_gap`
  - 默认值：`min_margin_gap=0.6`
- `_compute_rank_score` 及其输出字段已移除。

### 2.5 CLI 与文档

- `ssqpg` CLI 已同步新流程与新参数接口。  
- README 已改为 `python -m src.ssqpg.cli ...` 作为主启动方式，并更新新的分段调度示例。  
- `pyproject.toml` 中判定工具脚本更新为 `qa-verifier = "qa_judge.verifier:app"`。

---

> 维护说明：本节“近期更新（滚动）”后续按增量持续替换，不再叠加过期历史细节。
